-- Create a final table with all classifications and calculated columns,
-- used as the source table for the Tableau Public dashboard
CREATE TABLE final_export AS
SELECT * FROM "T-shirts and Polos dataset";
